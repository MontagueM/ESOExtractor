"""
From the first type 10 read:
----
mesh count of
    type 10
    vertex offset
    offset that has the vertex count
empty
mesh count of
    parts definition
    index offset
    offset that has the index count and part count
empty
mesh count of
    mesh name
    offset that has the vertex count
    offset that has the part count/index count
    unk
    unk
    more unk ...
    bone names ...
    maybe resets after that?
"""